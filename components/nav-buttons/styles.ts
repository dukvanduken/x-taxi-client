import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    position: 'absolute',
    bottom: '40%',
    right: 20,
  },

  btnBg: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#fff',
    borderWidth: 3,
    borderColor: '#ccc',
    height: 60,
    width: 60,
    borderRadius: 100,
  },

  btnImage: {
    width: 40,
    height: 40,
  }
})
